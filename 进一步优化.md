下面给出几个循序渐进、可并行开展的优化方向，均围绕「将 Ensemble AUC 推高至 ≥ 0.58」这一 Phase-2 目标展开。可根据团队资源按优先级挑选实施。

1. 数据层面  
    a. 数据范围扩充：  
    • 延伸时间轴至最新（2025-06 以后的 1h K 线），同步补齐 funding rate、open interest 等衍生品特征。  
    • 横向加入 ETH、SOL 等高流动性合约，做多资产联合训练或迁移学习。  
    b. 标签工程：  
    • 尝试 2-3 根 K 线 / 不同窗口累计收益方向（二分类或回归）。  
    • 引入对数收益阈值分级，改多分类，捕获震荡场景。  
    c. 不平衡处理：  
    • 采用 time-aware SMOTE / dynamic class weighting，在不破坏时序结构的前提下平衡样本。

2. 特征工程  
    a. Top-k 自动搜索：  
    • 基于现有 399 → 200 方案，继续用交叉验证 + Shapley 重要度做递进筛选 (Top-150 / 100 / 50)，观察性能与训练时长折衷。  
    b. 新特征注入：  
    • 深层订单簿统计（价差、档位压强）；  
    • 资金费率、持仓量、基差斜率等衍生品结构信息；  
    • 价格行为特征（K 线形态编码、趋势强度、布林带偏离度）。  
    c. 自动特征生成：  
    • 考虑使用 deep feature synthesis (featuretools) 或 tsfresh，先离线跑全量，再配合白名单过滤。

3. 模型与超参  
    a. 系统化 HPO：  
    • Optuna/Hyperopt 全局搜索 LightGBM、CatBoost、XGBoost，关注 learning_rate 与 num_leaves/max_depth 的协同。  
    • 重点挖掘 CatBoost 的类类别编码 (one-hot =None vs Ordered)。  
    b. 新模型引入：  
    • TabNet、FT-Transformer、SAINT 等表格深度模型；  
    • 传统模型的 GAM、Explainable Boosting Machine (EBM) 以增添可解释性。  
    c. 训练技巧：  
    • 时间序列 k-fold (rolling window) 替代单一 Val，降低过拟合；  
    • 目标扰动（label smoothing ≈ 0.05）以提升泛化。

4. 集成策略  
    a. 元学习加权：  
    • 用 LogisticRegression / Ridge 作为二层模型或采用 StackingCVRegressor。  
    b. 动态加权：  
    • 依据验证集近期窗口 (近 3 个月) 计算权重，模拟线上滚动更新。  
    c. 模型剪枝：  
    • 若新模型纳入后贡献 < 1e-3 AUC，可自动剔除，保持推理效率。

5. 线上部署与监控  
    a. 数据漂移报警：  
    • 建立特征分布 / PSI 实时监控，触发自动重训。  
    b. 模型版本治理：  
    • MLflow 或 BentoML 记录所有实验 → 线上灰度发布、AB Test。  
    c. 推理优化：  
    • 为 CatBoost / LightGBM 导出 C++ so / ONNX，减小延迟。

6. 研发流程  
    a. 代码与文档：  
    • 将 DEFAULT_MODEL_CONFIGURATION.md 与 ChanMLTrainer._get_default_config() 通过 CI 检查保持一致；  
    • 日志落盘时追加 git commit hash，方便回溯。  
    b. 自动化实验：  
    • 提供 Script/run_hpo.py、一键跑 10-20 组超参；  
    • 训练-验证-记录-汇总自动化，减少手工干预。

建议优先顺序：  
1) 数据完善 → 2) 特征扩充/筛选 → 3) 系统化 HPO → 4) 升级集成 → 5) 部署监控。  
按照此路线迭代，每一步都应以验证集 / 回测收益为硬指标，确保 AUC 与业务指标同步提升。